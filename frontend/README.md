# How to run (4 terminals)
1. cd backend && npx hardhat node
1. cd backend && npx hardhat run scripts/deploy.js --network localhost
1. cd backend && node server.js
1. cd frontend && npm run dev → http://localhost:8080